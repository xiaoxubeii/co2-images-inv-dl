# @package _global_

defaults:
    - override /model: inversion

exp_name: inv_lip

data:
    path:
        train:
            name: all_hotspots_but_lip
        valid:
            name: all_hotspots_but_lip
    input:
        chan_1: xco2_prec
        chan_2: None
        # chan_3: None
        # chan_4: None
        chan_3: v_wind
        chan_4: u_wind
        # chan_4: weighted_plume
callbacks:
    model_checkpoint: false
model:
    # name: cnn-lstm
    name: essential
training:
    max_epochs: 10

sweep: true
