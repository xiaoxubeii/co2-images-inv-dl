# @package _global_

defaults:
    - override /model: emiss_trans

data:
    init:
        cutoff_size: 64
        window_length: 2
        shift: 1
    path:
        train:
            name: all_hotspots_but_lip
        valid:
            name: all_hotspots_but_lip
    # input:
    #     # chan_1: xco2_prec
    #     # chan_2: no2
    #     # chan_3: v_wind
    #     # chan_4: u_wind
    #     # chan_5: weighted_plume
    #     chan_1: v_wind
    #     chan_2: u_wind
callbacks:
    model_checkpoint: false
training:
    max_epochs: 10
    batch_size: 32

sweep: true
wandb:
    project_name: co2-emission-estimation
    key: 69fea85817147f437d88815d733f3e2ed9799c12
